import 'package:flutter/material.dart';

class Pokemons extends StatelessWidget {
  const Pokemons({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text("All pokemons"),);
  }
}
